class Solution(object):
    def maxSubArray(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        current=best=nums[0]
        for i in nums[1:]:
            current=max(i,current+i)
            best= max(best,current)
        return best


        
