class Solution(object):
    def findMissingElements(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        minno=min(nums)
        maxno=max(nums)
        res=[]
        for x in range(minno,maxno):
            if x not in nums:
                res.append(x)
        return res
