class Solution(object):
    def finalValueAfterOperations(self, operations):
        """
        :type operations: List[str]
        :rtype: int
        """
        res=0
        for x in operations:
            if x=="--X" or x=="X--":
                res=res-1
            elif x=="++X" or x=="X++":
                res=res+1
        return res
