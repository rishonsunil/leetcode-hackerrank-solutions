class Solution(object):
    def shuffle(self, nums, n):
        """
        :type nums: List[int]
        :type n: int
        :rtype: List[int]
        """
        arr=[]
        for x in range(len(nums)/2):
            arr.append(nums[x])
            arr.append(nums[x+n])
        return arr
